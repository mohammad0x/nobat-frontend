/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental:{
        serverActions : true
    }
}

module.exports = nextConfig
module.exports = {
    images: {
        domains: ['images.unsplash.com'],
    },
}